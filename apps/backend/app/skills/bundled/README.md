# Bundled Skills

This directory contains skills shipped with the backend sidecar.

- Each bundled skill must live in a child directory.
- The child directory must contain a `SKILL.md`.
- The loader only considers direct child directories with `SKILL.md`.
- Files in this tree are data only; they are never executed by the loader.
